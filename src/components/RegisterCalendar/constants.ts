export enum TIME_SELECT_TYPE {
    RECOMEND = "RECOMEND",
    SELF = "SELF",
}

export const HOURS = Array(24).fill(0).map((_, index) => index)

export const MINUTES = Array(60).fill(0).map((_, index) => index)
